# Lloyd Python dependencies — human-edited intent (loose pins).
# For a reproducible rebuild, install from the frozen snapshot instead:
#   python -m venv .venvs/lloyd && .venvs/lloyd/bin/pip install -r requirements.lock
# Use this file only when intentionally upgrading; then refreeze
# (regeneration command in requirements.lock header).

# FastAPI server
fastapi>=0.115.0
uvicorn>=0.34.0

# MCP server framework
mcp>=1.0.0

# HTTP tools
httpx>=0.27.0
ddgs>=6.0.0

# Data handling
pyyaml>=6.0.0

# Browser control
playwright>=1.58.0

# Discord bot
discord.py>=2.3.0
