# Lloyd Python dependencies — human-edited intent (loose pins).
# For a reproducible rebuild, install from the frozen snapshot instead:
#   python -m venv .venvs/lloyd && .venvs/lloyd/bin/pip install -r requirements.lock
# Use this file only when intentionally upgrading; then refreeze
# (regeneration command in requirements.lock header).

# FastAPI server
fastapi>=0.115.0
uvicorn>=0.34.0

# MCP server framework. 2.x implements the 2026-07-28 spec: stateless
# core, Streamable HTTP, protocol-level list caching. Lower-bounded at
# 2.1 because agent_mcp/main.py registers handlers as Server(...)
# constructor arguments, which 1.x does not accept, and reads model
# fields by their snake_case names. Upper-bounded at 3 on principle.
mcp>=2.1.0,<3

# HTTP tools
httpx>=0.27.0
ddgs>=6.0.0
# Readability extraction for http_fetch. Rides lxml; keeps headings, lists,
# tables and link hrefs, which the hand-rolled HTMLParser extractor dropped.
trafilatura>=2.2.0
# PDF text extraction for http_fetch; a PDF URL used to come back as
# decoded binary.
pymupdf>=1.28.0

# Data handling
pyyaml>=6.0.0

# Browser control
playwright>=1.58.0

# Discord bot
discord.py>=2.3.0
