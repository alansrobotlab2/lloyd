"""The 24 worktree-prefixed Bash calls of autoimplement round SM_20260908_165950.

Verbatim from `sessions/20260908_165220_backlogi_365d.json`, the round that
implemented backlog #377. Every one opens `cd /home/<user>/lloyd-work/
SM_20260908_165950/home/lloyd &&`, and the observer fired its deterministic
repetition guard five times across them — on five calls doing five unrelated
things — then hit its budget cap at the moment the round reached its gate.

The shared term every one of those injects named was `sm_20260908_165950`,
which exists in the signature only because of the `cd` preamble.

Kept as data rather than a hand-written approximation: the failure was in how
these exact strings tokenise, and a paraphrase would not reproduce it.
"""

WORKTREE_BASH_CALLS = [
    (45, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && grep -rn "ANTICOMPLIANCE" --include="*.py" . | grep -v __pycache__ | head; echo "=== tests dir listing for prompt ==="; ls tests | grep -i prompt; echo "=== pytest.ini ==="; cat pytest.ini; echo "=== collected count check ==="; grep -rn "collected" ../..//home/lloyd/scripts/autoimplement/gate.py | sed -n \'1,6p\''),
    (46, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && sed -n \'1,40p\' prompt_builder.py; echo "..... 225-250 ....."; sed -n \'225,250p\' prompt_builder.py; echo "..... 310-345 ....."; sed -n \'306,330p\' prompt_builder.py'),
    (49, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && python3 - <<\'PY\'\nfrom pathlib import Path\np = Path(\'tests/test_prompt_surface_budget.py\')\nt = p.read_text()\n\nt = t.replace(\'\'\'pytestmark = pytest.mark.skipif(\n    not SOUL.exists(), reason="live vault prompt surface not on disk here"\n)\'\'\', \'\'\'# Only the invariant group below depends on the real vault; the prompt_builder\n# and prefetch behaviour tests build a fake one and always run.\nvault_only = pytest.mark.skipif(\n    not SOUL.exists(), reason="live vault prompt surface not on disk here"\n)\'\'\')\n\nfor name in ("test_gate_stack_is_a_minority_of_the_contract",\n             "test_gate_sections_still_exist",\n             "test_trim_kept_the_load_bearing_behaviours",\n             "test_prohibition_line_ratio_is_under_the_ceiling",\n             "test_memory_md_is_not_a_copy_of_the_contract",\n             "test_anti_compliance_frame_lives_in_exactly_one_place",\n             "test_section_audit_covers_every_section"):\n    old = f"def {name}("\n    assert old in t, name\n    t = t.replace(old, f"@vault_only\\n{old}", 1)\n\nt = t.replace(\'\'\'    with caplog.at_level(logging.INFO, logger=pb.logger.name):\n        prompt = pb.build_system_prompt(session_id="turn1")\n    assert pb.build_system_prompt()  # no session_id: no log line, same output\n    lines = [r.getMessage() for r in caplog.records if "PROMPT_BUDGET" in r.getMessage()]\n    assert len(lines) == 1, lines\'\'\', \'\'\'    with caplog.at_level(logging.INFO, logger=pb.logger.name):\n        prompt = pb.build_system_prompt(session_id="turn1")\n        silent = pb.build_system_prompt()   # no session_id -> no log line\n    assert silent == prompt, "logging changed the prompt it logged"\n    lines = [r.getMessage() for r in caplog.records if "PROMPT_BUDGET" in r.getMessage()]\n    assert len(lines) == 1, lines\'\'\')\np.write_text(t)\nprint("patched")\nPY\ngrep -c "@vault_only" tests/test_prompt_surface_budget.py'),
    (50, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && grep -rn "_load_memories\\|_load_soul\\|build_system_prompt(" --include="*.py" tests/ app/ scripts/ eval/ agent_mcp/ workers/ | grep -v __pycache__ | head -20; echo "=== overlay test memory assertions ==="; grep -n "memory\\|MEMORY" tests/test_prompt_builder_overlay.py | head -20'),
    (55, "cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && sed -n '255,420p' prompt_builder.py"),
    (56, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && git diff prompt_builder.py | sed -n \'1,80p\'; echo "@@@@@@ PREFETCH @@@@@@"; git diff prefetch.py; echo "@@@@@@ MESSAGES @@@@@@"; git diff app/routers/messages.py app/routers/voice.py'),
    (58, "cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && sed -n '260,335p' prompt_builder.py"),
    (59, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && grep -n "ANTICOMPLIANCE_DIRECTIVE\\|return \\"\\\\\\\\n\\\\\\\\n\\".join(parts)\\|def _load_soul\\|def _load_memories\\|parts.append(f\\"## {filename}" prompt_builder.py | sed -n \'1,20p\'; echo "=== tail of build ==="; sed -n \'/^    parts: list\\[str\\] = list(components.values())/,/^def _load_soul/p\' prompt_builder.py | tail -20'),
    (64, "cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && sed -n '1180,1205p' prefetch.py"),
    (65, "cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && sed -n '1201,1250p' prefetch.py"),
    (66, "cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && sed -n '990,1032p' prefetch.py"),
    (71, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && python3 - <<\'PY\'\nfrom pathlib import Path\nedits = [\n ("app/routers/messages.py",\n  "    system_prompt = build_system_prompt(\\n        todos=session_todos, plan=session_plan, goal=session_goal,\\n    )",\n  "    system_prompt = build_system_prompt(\\n        todos=session_todos, plan=session_plan, goal=session_goal,\\n        session_id=session_id,\\n    )"),\n ("app/routers/messages.py",\n  "    system_prompt = build_system_prompt(\\n        todos=existing.get(\\"todos\\") or [], plan=plan, goal=goal,\\n    )",\n  "    system_prompt = build_system_prompt(\\n        todos=existing.get(\\"todos\\") or [], plan=plan, goal=goal,\\n        session_id=session_id,\\n    )"),\n ("app/routers/messages.py",\n  "    system_prompt = build_system_prompt(\\n        todos=_load_session_todos(session_id), plan=sync_session_plan,\\n    )",\n  "    system_prompt = build_system_prompt(\\n        todos=_load_session_todos(session_id), plan=sync_session_plan,\\n        session_id=session_id,\\n    )"),\n ("app/routers/voice.py",\n  "    system_prompt = build_system_prompt(",\n  "    system_prompt = build_system_prompt(\\n        session_id=session_id,"),\n]\nfor path, old, new in edits:\n    p = Path(path); t = p.read_text()\n    n = t.count(old)\n    assert n >= 1, (path, old[:50])\n    t = t.replace(old, new, 1)\n    p.write_text(t)\n    print("patched", path, "| occurrences were", n)\nPY\ngrep -n "build_system_prompt(" -A3 app/routers/voice.py | head -12'),
    (73, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && grep -n "^from prefetch import\\|^import prefetch\\|from prefetch import" app/routers/messages.py | head -5; echo "=== prefetch import line context ==="; sed -n "$(grep -n \'from prefetch import\' app/routers/messages.py | head -1 | cut -d: -f1),+4p" app/routers/messages.py'),
    (74, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && .venvs/lloyd/bin/python -m pyflakes prompt_builder.py prefetch.py tests/test_prompt_surface_budget.py app/routers/messages.py app/routers/voice.py 2>&1 | head -10; echo "PYFLAKES_EXIT=$?"; HOME=/home/alansrobotlab .venvs/lloyd/bin/python -m pytest tests/test_prompt_surface_budget.py tests/test_prompt_builder_overlay.py -q 2>&1 | tail -25'),
    (75, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && V=/home/alansrobotlab/lloyd/.venvs/lloyd/bin/python; $V -m pyflakes prompt_builder.py prefetch.py tests/test_prompt_surface_budget.py app/routers/messages.py app/routers/voice.py; echo "PYFLAKES_EXIT=$?"; PYTHONPATH=$PWD $V -m pytest tests/test_prompt_surface_budget.py tests/test_prompt_builder_overlay.py -q 2>&1 | tail -25'),
    (76, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && V=/home/alansrobotlab/lloyd/.venvs/lloyd/bin/python; echo "=== BASE prompt_builder pyflakes ==="; git show HEAD:prompt_builder.py > /tmp/base_pb.py && $V -m pyflakes /tmp/base_pb.py; echo "=== BASE messages.py count ==="; git show HEAD:app/routers/messages.py > /tmp/base_msg.py && $V -m pyflakes /tmp/base_msg.py | wc -l; echo "=== BASE voice ==="; git show HEAD:app/routers/voice.py > /tmp/base_voice.py && $V -m pyflakes /tmp/base_voice.py'),
    (77, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && python3 - <<\'PY\'\nfrom pathlib import Path\n\np = Path(\'app/routers/messages.py\'); t = p.read_text()\nold = "from prefetch import prefetch_context_async"\nnew = "from prefetch import prefetch_context_async, log_turn_prompt_budget"\nassert t.count(old) == 1\np.write_text(t.replace(old, new)); print("import added")\n\np = Path(\'tests/test_prompt_surface_budget.py\'); t = p.read_text()\nold = \'\'\'    uncovered = []\n    for heading, _ in _sections(soul_text):\n        # Two words: the audit table is keyed on the pre-trim heading for the\n        # sections that were renamed.\n        key = " ".join(re.findall(r"[\\\\w&]+", heading)[:2]).lower()\n        if key and key not in text:\n            uncovered.append(heading)\'\'\'\nnew = \'\'\'    haystack = _normalize(text)\n    uncovered = []\n    for heading, _ in _sections(soul_text):\n        # Two words: the audit table is keyed on the pre-trim heading for the\n        # sections that were renamed, so match on a punctuation-free prefix.\n        key = " ".join(_normalize(heading).split()[:2])\n        if key and key not in haystack:\n            uncovered.append(heading)\'\'\'\nassert t.count(old) == 1, "audit loop not found"\nt = t.replace(old, new)\nt = t.replace(\'\'\'def _shared_line_share(\'\'\', \'\'\'def _normalize(text: str) -> str:\n    """Lowercase, every run of non-alphanumerics to a single space.\n\n    Headings carry hyphens, ampersands and parenthetical flags; the audit table\n    is prose. Comparing raw strings makes `Anti-Compliance` and `anti compliance`\n    different sections, which would report a covered section as missing.\n    """\n    return re.sub(r"[^a-z0-9&]+", " ", text.lower()).strip()\n\n\ndef _shared_line_share(\'\'\', 1)\np.write_text(t); print("test normalizer added")\nPY\nV=/home/alansrobotlab/lloyd/.venvs/lloyd/bin/python; $V -m pyflakes prompt_builder.py prefetch.py tests/test_prompt_surface_budget.py app/routers/voice.py app/routers/messages.py | grep -v "imported but unused\\|think_level"; echo "---"; PYTHONPATH=$PWD $V -m pytest tests/test_prompt_surface_budget.py tests/test_prompt_builder_overlay.py -q 2>&1 | tail -6'),
    (78, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && echo "=== ACCEPTANCE CHECK (live) ===" && python3 - <<\'PY\'\nimport re, subprocess\nfrom pathlib import Path\nV=Path(\'/home/alansrobotlab/obsidian/lloyd\')\nGATE=(\'L0 SAFETY INTERRUPT GATE\',\'L0 PRE-COMMIT SAFETY CHECKLIST\',\'ATOMIC BLOCK SIGNAL\',\'BLOCK SIGNAL\',\'ZERO PREAMBLE\',\'ZERO-PREAMBLE\',\'STRICT OUTPUT SHAPE GATE\')\nt=(V/\'SOUL.md\').read_text()\nlines=t.split(\'\\n\'); heads=[(i,l) for i,l in enumerate(lines,1) if l.startswith(\'## \')]\ntot=len(t.encode()); g=0\nfor i,(ln,h) in enumerate(heads):\n    end=heads[i+1][0]-1 if i+1<len(heads) else len(lines)\n    if h[3:].startswith(GATE): g+=len(\'\\n\'.join(lines[ln-1:end]).encode())\nprint(f"(1) audit artifact: {len(list((V/\'reviews\').glob(\'*soul*audit*.md\')))} file(s); gate stack {g}/{tot} = {g/tot*100:.1f}%  -> {\'PASS\' if g/tot<=0.5 else \'FAIL\'}")\nr=subprocess.run(\'cmp <(head -c $(wc -c < /home/alansrobotlab/obsidian/lloyd/SOUL.md) /home/alansrobotlab/obsidian/lloyd/MEMORY.md) /home/alansrobotlab/obsidian/lloyd/SOUL.md; echo $?\',shell=True,capture_output=True,text=True)\nh1=(V/\'MEMORY.md\').read_text().split(\'\\n\')[0]\nprint(f"(2) cmp exit={r.stdout.strip()} head-1={h1!r} -> {\'PASS\' if r.stdout.strip()!=\'0\' and h1==\'# Lloyd Long-Term Memory\' else \'FAIL\'}")\nc=subprocess.run(\'grep -c "Certainly" "$0" "$1"\',shell=True,capture_output=True,text=True)\nn=sum(1 for f in (Path(\'/home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd/prompt_builder.py\'), V/\'SOUL.md\') for _ in [0] )\ntot_c=sum(f.read_text().count(\'Certainly\') for f in (Path(\'prompt_builder.py\'), V/\'SOUL.md\'))\nprint(f"(3) \'Certainly\' total = {tot_c} -> {\'PASS\' if tot_c==1 else \'FAIL\'}")\nsrc=Path(\'prompt_builder.py\').read_text()+Path(\'prefetch.py\').read_text()\nhits=[k for k in (\'PROMPT_BUDGET_CHARS\',\'prompt_token_estimate\',\'turn_total_chars\',\'components[\') if k in src]\nprint(f"(4) size grep hits: {hits} -> {\'PASS\' if len(hits)>=3 else \'FAIL\'}")\nPY\ngit add -A && git -c user.name=alansrobotlab -c user.email=gestalt73@gmail.com commit -q -m "prompt surface: measure it, state the frame once, refuse the paste (#377)\n\nThe identity surface grew to 62% gate stack and MEMORY.md was overwritten with a\nverbatim copy of SOUL.md, so every turn paid ~4.4K duplicate tokens and nothing\nin the code could see it: no code measured prompt size at all (#466), the\nanti-compliance rules shipped twice in two diverged wordings (#465), and the\npasted contract reached the model twice per turn (#464).\n\n- prompt_builder: PROMPT_BUDGET_CHARS, measure_prompt() and log_prompt_size()\n  emit a per-component char/token line per build; prefetch gains\n  log_turn_prompt_budget() for the injected half and the turn total. The chat,\n  ambient, sync and voice handlers pass session_id so there is one line per turn.\n  Nothing is truncated: a size that exceeds the budget is a loud number, not a\n  silently cut identity file.\n- The ANTICOMPLIANCE_DIRECTIVE constant is deleted. SOUL.md\'s\n  \'Anti-Compliance Directive\' is now the single home of those six rules — it is\n  the copy the prompt-promotion path can actually edit, and the Python copy had\n  already diverged (it lacked Contradiction Handling).\n- _load_memories() drops a run of >=40 consecutive contract lines from a memory\n  file and logs a WARNING naming the file and the byte count, so the #464 paste\n  cannot silently return. Longest-run, not total count, so a note quoting two\n  contract lines survives; blank lines do not break a run, or a markdown paste\n  would measure as runs of 1-3 and slip through.\n- tests/test_prompt_surface_budget.py pins the #377 acceptance check against the\n  live vault (skipped where that vault is absent, so no fixture can satisfy it),\n  plus the load-bearing safety/shape markers, the budget telemetry and the paste\n  guard. The vault half of #377 is vault commit 010d233." && git log --oneline -1'),
    (84, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && PYTHONPATH=$PWD /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python scripts/autoresearch/bench_runner_sdk.py --task bench_006_contradiction_check --task bench_008_adversarial_gap --task bench_009_adversarial_probe --task bench_010_safety_destructive --log-level WARNING > /tmp/bench377_post.json 2>/tmp/bench377_post.err; echo "EXIT=$?"; tail -c 2500 /tmp/bench377_post.json; echo; tail -5 /tmp/bench377_post.err'),
    (85, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && PYTHONPATH=$PWD /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python -m scripts.autoresearch.bench_runner_sdk --task bench_006_contradiction_check --task bench_008_adversarial_gap --task bench_009_adversarial_probe --task bench_010_safety_destructive --log-level WARNING > /tmp/b377_post.json 2>/tmp/b377_post.err; echo "EXIT=$?"; cat /tmp/b377_post.json | tail -c 3000; echo "--- err"; tail -8 /tmp/b377_post.err'),
    (86, 'cd /home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd && PYTHONPATH=$PWD timeout 420 /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python eval/run_tool_choice_eval.py --label soul377-post 2>&1 | tail -20'),
    (89, 'W=/home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd; ls -t $W/eval/baselines/tool-choice/ 2>/dev/null | head -3; F=$(ls -t $W/eval/baselines/tool-choice/*.json 2>/dev/null | head -1); echo "F=$F"; /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python -c "\nimport json\nd=json.load(open(\'$F\'))\nprint({k:d[k] for k in d if not isinstance(d[k],(list,dict))})\ns=d.get(\'summary\') or {}\nprint(\'summary:\',{k:s[k] for k in s if not isinstance(s[k],(list,dict))})\n" 2>&1 | head -10; echo "=== live baselines for comparison ==="; ls -t ~/lloyd/eval/baselines/tool-choice/*.json 2>/dev/null | head -3 | while read f; do echo -n "$f "; /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python -c "import json;d=json.load(open(\'$f\'));s=d.get(\'summary\') or d;print({k:v for k,v in s.items() if isinstance(v,(int,float))})"; done'),
    (90, 'W=/home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd; /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python - <<PY\nimport json, glob\nf="$W/eval/baselines/tool-choice/soul377-post-20260908-102548.json"\nd=json.load(open(f))\nrs=d.get(\'results\') or d.get(\'queries\') or []\nprint("keys:", list(d.keys()))\nif rs:\n    k=[x for x in rs[0].keys()]\n    print("result keys:", k)\n    for cand in (\'correct\',\'selected_correct\',\'ok\',\'match\'):\n        if cand in rs[0]:\n            n=sum(1 for r in rs if r.get(cand)); print(f"{cand}: {n}/{len(rs)}")\n    for r in rs[:3]: print({x:str(r.get(x))[:60] for x in k})\nPY'),
    (91, 'W=/home/alansrobotlab/lloyd-work/SM_20260908_165950/home/lloyd; /home/alansrobotlab/lloyd/.venvs/lloyd/bin/python - <<PY\nimport json\nd=json.load(open("$W/eval/baselines/tool-choice/soul377-post-20260908-102548.json"))\nprint("summary:", d["summary"])\nrs=d["records"]; ks=sorted(rs[0].keys()); print("record keys:", ks)\nfor c in ("correct","selected_correct","ok","match","passed"):\n    if c in rs[0]:\n        print(c, sum(1 for r in rs if r.get(c)), "/", len(rs))\nPY'),
]
