{
  "id": "web-local",
  "name": "Local Web Tools",
  "description": "web_search via Google + web_fetch via HTTP/Readability — no API keys needed",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
