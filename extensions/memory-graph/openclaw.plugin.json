{
  "id": "memory-graph",
  "name": "Memory Graph",
  "description": "Unified memory context prefill. Runs tag matching and vector search in parallel before the first LLM call, merges and deduplicates results into a single context block. Also provides tag_search, tag_explore, and vault_overview tools.",
  "version": "3.0.0",
  "main": "index.ts",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
