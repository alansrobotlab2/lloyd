# `lloyd_rpc` (P9), 2026-09-25

**Verdict: do not promote; `harness.rpc.enabled` stays false.**

Runner: `eval/run_rpc_eval.py --live-bash` — the one-time exception to the
eval sandbox rule Alan granted on 2026-09-25 (not a standing one). Worker pool
paused, primary lock held, vault snapshot `1d4fa188` (6,131 files) taken first;
the vault tripwire stayed clear, and no eval Bash command wrote anything (the
four vault files changed in the window belong to the daily note, the backlog
and the YouTube digest). 3 tasks × 5 reps × arms `off` / `off_rep` / `on`,
interleaved. Raw: `eval/baselines/rpc-2026-09-25/`.

| task | prompt tokens, on vs off | pass off / A/A / on | rpc calls (on) |
|---|---|---|---|
| backlog board pass | −1.7% (i.e. +1.7% tokens) | 1.0 / 1.0 / 1.0 | 0 |
| grep + read synthesis | ≈ 0 | 1.0 / 0.8 / 1.0 | 0 |
| vault front-matter reconcile | −0.4% | 0.0 / 0.0 / 0.0 | 0 |

Token wins: 0 of 3 (rule: ≥ 30% on ≥ 2). Zero Bash timeouts.

## Reading

The model never called `lloyd_rpc`: 0 nested calls in 15 `on` runs. The
feature was in reach — the `on` arm's system prompt carries the P9 hint
(verified), and its Bash calls are stamped — but one paragraph in a 111k-char
prompt does not change how Qwen3.8 approaches a task it can do with Grep and
Read. So this measures adoption, not the mechanism, and adoption is zero.
Next step if it is worth pursuing: a skill (or a worked example in the hint)
that shows the pattern on exactly these task shapes, then re-measure — which
needs another live-Bash exception from Alan. The vault-reconcile task failed
in every arm alike; its ground truth should be checked before it is reused.
